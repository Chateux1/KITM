whoami
whois

**cat more less head tail filename 

cat /etc/passwd
cat /etc/shadow
cat /etc/group

groupadd 
sudo groupadd name

groupmod -n -g
find / -nogroup

groupdel name

useradd -D +
	-g for GROUP
	-G for additional GROUPS separated by comas
	-b for HOME directory root
	-f for INACTIVE default -1 (days for password to expire)
	-e for EXPIRE days for user to expire
	-s for SHELL default /bin/bash (chsh for regular not admin user)
	-k for SKEL defalut /etc/skel
	-m for create user HOME directory (not needed by default, only with -k)
	-K for NOT create HOME directory
	-c for comment in ''

		+ user_name
sudo useradd -D -f 30
sudo useradd -m -g users -G additionalGroup1, Group2, Group3 -s /bin/bash user_name

usermod
-------

Short option 		Long option 				Description
-c 					COMMENT 					Sets the value of the GECOS or comment field to COMMENT.
-d HOME_DIR 		--home HOME_DIR 			Sets a new home directory for the user.
-e EXPIRE_DATE 		--expiredate EXPIRE_DATE 	Set account expiration date to EXPIRE_DATE.
-f INACTIVE 		--inactive INACTIVE 		Set account to permit login for INACTIVE days after password expires.
-g GROUP 			--gid GROUP 				Set GROUP as primary group.
-G GROUPS 			--groups GROUPS 			Set supplementary groups to a list specified in GROUPS.
-a 					--append 					Append the user's supplemental groups with those specified by -G.
-h 					--help 						Show the help for usermod.
-l NEW_LOGIN 		--login NEW_LOGIN 			Change the user's login name.
-L 					--lock 						Lock the user account.
-s SHELL 			--shell SHELL 				Specify the login shell for the account.
-u NEW_UID 			--uid NEW_UID 				Specify the user's UID to be NEW_UID.
-U 					--unlock 					Unlock the user account.

usermod -aG mokykla moksleivis

sudo passwd moksleivis

userdel +
	-f for force delete
	-r for remove files/folders
		+ user_name
sudo userdel -fr user_name

gpasswd +
	-a for add to group
	-d for delete from group
		+ user_name
sudo gpasswd -a user_name group_name

useradd >>> /etc/login.defs >>> edit manually
useradd -D >>> /etc/default/useradd >>> edit through useradd command with -D
grep -Ev '^#|^$' /etc/login.defs >>> use grep to see only neccessary contents

1-2

